import { NavBar } from "./NavBar"

export const Header = () => {
    return(
        <header className="flex place-content-between mb-6">
            <h1>LOGO</h1>
            <NavBar />
        </header>
    )
}